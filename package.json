{
  "name": "esbuild-tsconfig",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild component.ts --bundle --outfile=test.js --tsconfig=tsconfig.json --target=es2020",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.1.1"
  },
  "devDependencies": {
    "esbuild": "0.19.12",
    "typescript": "^5.3.3"
  }
}
